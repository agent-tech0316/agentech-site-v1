import type { NextConfig } from "next";
const config: NextConfig = { output: "export", assetPrefix: "https://navi-app-download.wesleyfan2015.chatgpt.site", images: { unoptimized: true }, trailingSlash: true };
export default config;
